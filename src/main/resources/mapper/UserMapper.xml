<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dashan.news.mapper.UserMapper">
    <resultMap id="UserMap" type="com.dashan.news.pojo.User">
        <id property="userId" column="user_id"></id>
        <result property="userName" column="user_name"></result>
        <result property="email" column="email"/>
    </resultMap>
    <insert id="insert"></insert>

    <select id="select" resultMap="UserMap" parameterType="com.dashan.news.pojo.User">
        select user_id,user_name,email from user
        <where>
            <if test="userId != 0">
                user_id = #{userId}
            </if>
        </where>
    </select>
</mapper>